<nav class="navbar navbar-expand navbar-dark bg-dark static-top">
	
	<!-- Navbar -->
	<% if(!auth) { %>
	<a class="navbar-brand mr-1" href="/"><img src="/images/petcare.png" alt="" width="32" height="32" /> &nbsp; PetCare</a>
	<ul class="navbar-nav ml-auto">
		<li class="nav-item">
		  <a class="nav-link <% if (page === 'register') { %> active <% } %>" href="/register">Register</a>
		</li>
		<li class="nav-item">
			<a class="nav-link <% if (page === 'login') { %> active <% } %>" href="/login">Login</a>
		</li>
	</ul>
	<% } %>

	<!-- Navbar -->
	<% if(auth) { %>
		<% if (type === "admin") { %>
			<a class="navbar-brand mr-1" href="/admin_dashboard"><img src="/images/petcare.png" alt="" width="32" height="32" /> &nbsp; PetCare</a>
		<% } else { %>
			<a class="navbar-brand mr-1" href="/dashboard"><img src="/images/petcare.png" alt="" width="32" height="32" /> &nbsp; PetCare</a>
		<% } %>
	<ul class="navbar-nav ml-auto">
		<li class="nav-item dropdown no-arrow">
			<a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
				<i class="fas fa-user-circle fa-fw"></i>
			</a>
			<div class="dropdown-menu dropdown-menu-right" aria-labelledby="userDropdown">
				<span class="dropdown-item disabled"><b> <%= username %> </b></span>
				<a class="dropdown-item <%= page==="profile" ? "active" : "" %>" href="profile"><i class="fas fa-user"></i> My Profile</a>
				<a class="dropdown-item" href="/logout"><i class="fas fa-sign-out-alt"></i> Logout</a>
			</div>
		</li>
	</ul>
	<% } %>
</nav>